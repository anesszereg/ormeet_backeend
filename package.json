{
  "name": "ormeet",
  "private": true,
  "scripts": {
    "dev": "concurrently -n BACKEND,LANDING,FRONTEND -c blue,magenta,green \"npm run dev:backend\" \"npm run dev:landing\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run start:dev",
    "dev:landing": "cd landing-page && npm run dev -- -p 3001",
    "dev:frontend": "cd front-end && npm run dev -- --port 5173",
    "build": "npm run build:landing && npm run build:frontend",
    "build:landing": "cd landing-page && npm run build",
    "build:frontend": "cd front-end && npm run build",
    "install:all": "npm install && cd backend && npm install && cd ../landing-page && npm install && cd ../front-end && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "@emotion/react": "^11.14.0",
    "@emotion/styled": "^11.14.1",
    "@mui/material": "^7.3.7",
    "@mui/x-date-pickers": "^8.24.0",
    "dayjs": "^1.11.19",
    "leaflet": "^1.9.4",
    "next": "^16.1.6",
    "react": "^19.2.4",
    "react-datepicker": "^9.1.0",
    "react-dom": "^19.2.4",
    "react-leaflet": "^5.0.0"
  }
}
