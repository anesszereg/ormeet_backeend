# Application Configuration
NODE_ENV=development
PORT=3000
APP_URL=http://localhost:3000

# Database Configuration
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=postgres
DATABASE_NAME=event_organization_db

# JWT Configuration
# IMPORTANT: Change this to a secure random string in production
# Generate with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRATION=7d

# Resend Email Configuration (HTTP API - works on Render free tier)
# Get your API key at: https://resend.com/api-keys
RESEND_API_KEY=re_your_resend_api_key
EMAIL_FROM=Ormeet <onboarding@resend.dev>

# Frontend URL (for OAuth redirects)
FRONTEND_URL=http://localhost:5173

# CORS Configuration
CORS_ORIGIN=*

# Google OAuth Configuration
# Get credentials at: https://console.cloud.google.com/apis/credentials
# Add https://<your-backend-domain>/auth/google/callback as authorized redirect URI in Google Console
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=https://ormeet-backeend.onrender.com/auth/google/callback

# Facebook OAuth Configuration
# Get credentials at: https://developers.facebook.com/apps
FACEBOOK_APP_ID=your-facebook-app-id
FACEBOOK_APP_SECRET=your-facebook-app-secret
FACEBOOK_CALLBACK_URL=http://localhost:3000/auth/facebook/callback

# MessageBird SMS Configuration (for phone verification codes)
# Optional: if left blank, SMS codes are logged to the server console (dev fallback).
# Get credentials at: https://dashboard.messagebird.com/en/developers/access
MESSAGEBIRD_API_KEY=MWy6CL0kwXlp8PCP1vCYGdWl0yZJppxzwmOd
MESSAGEBIRD_ORIGINATOR=Ormeet

# Cloudinary Configuration (for image uploads)
# Get credentials at: https://console.cloudinary.com/settings/api-keys
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# pgAdmin Configuration (Optional)
PGADMIN_EMAIL=admin@admin.com
PGADMIN_PASSWORD=admin
PGADMIN_PORT=5050

